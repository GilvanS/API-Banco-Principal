eu estou criando uma api para treinamento e estou seguindo esse projeto Novo Swagger: API Banco Digital Focada em Fluxo de Abertura e Operações Essenciais Este Swagger elimina a necessidade de papéis/roles e centraliza as operações em um único "usuário" (que terá suas contas, cartões, etc.). A segurança agora é mais sobre o token de acesso que permite o usuário operar em suas próprias contas e recursos. Principais Mudanças e Adiçoes: Remoção de Roles/Perfis: Simplificando a autenticação e autorização para um único tipo de usuário (o proprietário das contas). Fluxo de Abertura de Conta: POST /onboarding/account: Um endpoint para iniciar a abertura de conta, criando o usuário e a primeira conta (Corrente ou Poupança). POST /onboarding/card: Solicitando o cartão após a abertura da conta. Endpoints de Transação: deposit, withdraw, transfer (já estavam, mas aprimorados). Novos Endpoints de Cartão: purchase (simulando compra), payment (simulando pagamento de fatura - para cartão de crédito, se implementado). Endpoints de Consulta: balance, statement, card-invoice. "Usuário" Abstrato: Para simplificar, o "usuário" é criado implicitamente na abertura da conta, e todas as operações subsequentes são para esse usuário (via token). code JSON { "swagger": "2.0", "info": { "title": "API Banco Digital de Teste - Fluxo Essencial", "description": "API simplificada para testes de integração com foco no fluxo de abertura de conta, operações transacionais (depósito, saque, transferência, compra, pagamento) e consultas básicas (saldo, extrato, fatura de cartão).", "version": "1.0.0" }, "host": "localhost:3000", "basePath": "/api/v1", "schemes": [ "http" ], "securityDefinitions": { "bearerAuth": { "type": "apiKey", "name": "Authorization", "in": "header", "description": "Token JWT com 'Bearer ' prefixo. Obtido após o login do usuário." } }, "paths": { "/auth/register": { "post": { "summary": "Registrar novo cliente e criar primeira conta", "description": "Endpoint inicial para um novo cliente se registrar. Cria um usuário, gera uma conta bancária (corrente ou poupança, ou ambas) e um token de acesso para futuras operações. Simula o onboarding completo.", "consumes": [ "application/json" ], "produces": [ "application/json" ], "parameters": [ { "name": "onboardingRequest", "in": "body", "required": true, "schema": { "$ref": "#/definitions/OnboardingRequest" } } ], "responses": { "201": { "description": "Cliente e conta(s) criados com sucesso. Retorna o token de acesso.", "schema": { "$ref": "#/definitions/OnboardingResponse" } }, "400": { "description": "Dados de registro inválidos (e.g., CPF já cadastrado, email inválido).", "schema": { "$ref": "#/definitions/ErrorResponse" } } } } }, "/auth/login": { "post": { "summary": "Login de cliente existente", "description": "Permite que um cliente já registrado faça login com suas credenciais e obtenha um novo token JWT.", "consumes": [ "application/json" ], "produces": [ "application/json" ], "parameters": [ { "name": "credentials", "in": "body", "required": true, "schema": { "$ref": "#/definitions/LoginRequest" } } ], "responses": { "200": { "description": "Login bem-sucedido. Retorna o token JWT.", "schema": { "$ref": "#/definitions/LoginResponse" } }, "400": { "description": "Requisição inválida.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "401": { "description": "Credenciais inválidas.", "schema": { "$ref": "#/definitions/ErrorResponse" } } } } }, "/accounts": { "get": { "summary": "Listar contas do cliente logado", "description": "Retorna todas as contas (corrente e/ou poupança) pertencentes ao cliente autenticado.", "security": [ { "bearerAuth": [] } ], "produces": [ "application/json" ], "responses": { "200": { "description": "Lista de contas do cliente.", "schema": { "type": "array", "items": { "$ref": "#/definitions/AccountResponse" } } }, "401": { "description": "Não autenticado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "404": { "description": "Cliente não possui contas.", "schema": { "$ref": "#/definitions/ErrorResponse" } } } } }, "/accounts/{accountId}/balance": { "get": { "summary": "Consultar saldo de uma conta", "description": "Retorna o saldo atual de uma conta específica do cliente autenticado.", "security": [ { "bearerAuth": [] } ], "produces": [ "application/json" ], "parameters": [ { "name": "accountId", "in": "path", "required": true, "type": "string", "format": "uuid", "description": "ID da conta para consultar o saldo." } ], "responses": { "200": { "description": "Saldo da conta.", "schema": { "$ref": "#/definitions/BalanceResponse" } }, "401": { "description": "Não autenticado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "403": { "description": "Não autorizado (conta não pertence ao cliente logado).", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "404": { "description": "Conta não encontrada.", "schema": { "$ref": "#/definitions/ErrorResponse" } } } } }, "/accounts/{accountId}/statement": { "get": { "summary": "Consultar extrato de transações de uma conta", "description": "Retorna o histórico de transações de uma conta específica do cliente autenticado, com opções de filtro por período e tipo.", "security": [ { "bearerAuth": [] } ], "produces": [ "application/json" ], "parameters": [ { "name": "accountId", "in": "path", "required": true, "type": "string", "format": "uuid", "description": "ID da conta para consultar o extrato." }, { "name": "startDate", "in": "query", "description": "Data de início para o extrato (formato YYYY-MM-DD)", "required": false, "type": "string", "format": "date" }, { "name": "endDate", "in": "query", "description": "Data de fim para o extrato (formato YYYY-MM-DD)", "required": false, "type": "string", "format": "date" }, { "name": "type", "in": "query", "description": "Tipo de transação (DEPOSITO, SAQUE, TRANSFERENCIA, COMPRA, PAGAMENTO)", "required": false, "type": "string", "enum": ["DEPOSITO", "SAQUE", "TRANSFERENCIA", "COMPRA", "PAGAMENTO"] } ], "responses": { "200": { "description": "Extrato de transações.", "schema": { "type": "array", "items": { "$ref": "#/definitions/TransactionResponse" } } }, "401": { "description": "Não autenticado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "403": { "description": "Não autorizado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "404": { "description": "Conta não encontrada.", "schema": { "$ref": "#/definitions/ErrorResponse" } } } } }, "/transactions/deposit": { "post": { "summary": "Realizar um depósito em conta", "description": "Deposita um valor em uma conta específica do cliente autenticado.", "security": [ { "bearerAuth": [] } ], "consumes": [ "application/json" ], "produces": [ "application/json" ], "parameters": [ { "name": "depositRequest", "in": "body", "required": true, "schema": { "$ref": "#/definitions/DepositWithdrawRequest" } } ], "responses": { "200": { "description": "Depósito realizado com sucesso.", "schema": { "$ref": "#/definitions/TransactionResponse" } }, "400": { "description": "Requisição inválida (e.g., valor negativo, conta inválida).", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "401": { "description": "Não autenticado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "403": { "description": "Não autorizado (conta não pertence ao cliente logado).", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "404": { "description": "Conta de destino não encontrada.", "schema": { "$ref": "#/definitions/ErrorResponse" } } } } }, "/transactions/withdraw": { "post": { "summary": "Realizar um saque de conta", "description": "Realiza um saque de uma conta específica do cliente autenticado, validando o saldo.", "security": [ { "bearerAuth": [] } ], "consumes": [ "application/json" ], "produces": [ "application/json" ], "parameters": [ { "name": "withdrawRequest", "in": "body", "required": true, "schema": { "$ref": "#/definitions/DepositWithdrawRequest" } } ], "responses": { "200": { "description": "Saque realizado com sucesso.", "schema": { "$ref": "#/definitions/TransactionResponse" } }, "400": { "description": "Requisição inválida (e.g., valor negativo, conta inválida).", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "401": { "description": "Não autenticado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "403": { "description": "Não autorizado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "404": { "description": "Conta de origem não encontrada.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "409": { "description": "Saldo insuficiente.", "schema": { "$ref": "#/definitions/ErrorResponse" } } } } }, "/transactions/transfer": { "post": { "summary": "Realizar uma transferência entre contas", "description": "Transfere um valor de uma conta de origem (do cliente logado) para uma conta de destino (pode ser de outro cliente).", "security": [ { "bearerAuth": [] } ], "consumes": [ "application/json" ], "produces": [ "application/json" ], "parameters": [ { "name": "transferRequest", "in": "body", "required": true, "schema": { "$ref": "#/definitions/TransferRequest" } } ], "responses": { "200": { "description": "Transferência realizada com sucesso.", "schema": { "$ref": "#/definitions/TransactionResponse" } }, "400": { "description": "Requisição inválida (e.g., valor negativo, contas iguais, conta de origem inválida).", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "401": { "description": "Não autenticado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "403": { "description": "Não autorizado (conta de origem não pertence ao cliente logado).", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "404": { "description": "Conta de origem ou destino não encontrada.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "409": { "description": "Saldo insuficiente na conta de origem.", "schema": { "$ref": "#/definitions/ErrorResponse" } } } } }, "/cards": { "post": { "summary": "Solicitar um novo cartão para uma conta existente", "description": "Cria um novo cartão (débito ou crédito) associado a uma conta existente do cliente autenticado. Pode ser solicitado para uma conta que já possui cartão.", "security": [ { "bearerAuth": [] } ], "consumes": [ "application/json" ], "produces": [ "application/json" ], "parameters": [ { "name": "cardCreateRequest", "in": "body", "required": true, "schema": { "$ref": "#/definitions/CardCreateRequest" } } ], "responses": { "201": { "description": "Cartão solicitado com sucesso.", "schema": { "$ref": "#/definitions/CardResponse" } }, "400": { "description": "Requisição inválida (e.g., tipo de cartão inválido, conta inválida).", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "401": { "description": "Não autenticado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "403": { "description": "Não autorizado (conta não pertence ao cliente logado).", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "404": { "description": "Conta não encontrada.", "schema": { "$ref": "#/definitions/ErrorResponse" } } } }, "get": { "summary": "Listar cartões do cliente logado", "description": "Retorna todos os cartões (débito e/ou crédito) pertencentes ao cliente autenticado.", "security": [ { "bearerAuth": [] } ], "produces": [ "application/json" ], "responses": { "200": { "description": "Lista de cartões do cliente.", "schema": { "type": "array", "items": { "$ref": "#/definitions/CardResponse" } } }, "401": { "description": "Não autenticado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "404": { "description": "Cliente não possui cartões.", "schema": { "$ref": "#/definitions/ErrorResponse" } } } } }, "/cards/{cardId}/block": { "patch": { "summary": "Bloquear um cartão", "description": "Altera o status de um cartão para 'BLOQUEADO'. O cartão deve pertencer ao cliente autenticado.", "security": [ { "bearerAuth": [] } ], "produces": [ "application/json" ], "parameters": [ { "name": "cardId", "in": "path", "required": true, "type": "string", "format": "uuid", "description": "ID do cartão a ser bloqueado." } ], "responses": { "200": { "description": "Cartão bloqueado com sucesso.", "schema": { "$ref": "#/definitions/CardResponse" } }, "401": { "description": "Não autenticado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "403": { "description": "Não autorizado (cartão não pertence ao cliente logado).", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "404": { "description": "Cartão não encontrado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "409": { "description": "Cartão já está bloqueado ou não pode ser bloqueado neste status.", "schema": { "$ref": "#/definitions/ErrorResponse" } } } } }, "/cards/{cardId}/unblock": { "patch": { "summary": "Desbloquear um cartão", "description": "Altera o status de um cartão para 'ATIVO' novamente. O cartão deve pertencer ao cliente autenticado.", "security": [ { "bearerAuth": [] } ], "produces": [ "application/json" ], "parameters": [ { "name": "cardId", "in": "path", "required": true, "type": "string", "format": "uuid", "description": "ID do cartão a ser desbloqueado." } ], "responses": { "200": { "description": "Cartão desbloqueado com sucesso.", "schema": { "$ref": "#/definitions/CardResponse" } }, "401": { "description": "Não autenticado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "403": { "description": "Não autorizado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "404": { "description": "Cartão não encontrado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "409": { "description": "Cartão já está desbloqueado ou não pode ser desbloqueado neste status.", "schema": { "$ref": "#/definitions/ErrorResponse" } } } } }, "/cards/{cardId}/purchase": { "post": { "summary": "Realizar uma compra com cartão", "description": "Simula uma transação de compra utilizando um cartão. Afeta o limite do cartão de crédito ou o saldo da conta para débito.", "security": [ { "bearerAuth": [] } ], "consumes": [ "application/json" ], "produces": [ "application/json" ], "parameters": [ { "name": "cardId", "in": "path", "required": true, "type": "string", "format": "uuid", "description": "ID do cartão usado na compra." }, { "name": "purchaseRequest", "in": "body", "required": true, "schema": { "$ref": "#/definitions/CardTransactionRequest" } } ], "responses": { "200": { "description": "Compra realizada com sucesso.", "schema": { "$ref": "#/definitions/TransactionResponse" } }, "400": { "description": "Requisição inválida (e.g., valor negativo, cartão bloqueado, limite/saldo insuficiente).", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "401": { "description": "Não autenticado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "403": { "description": "Não autorizado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "404": { "description": "Cartão não encontrado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "409": { "description": "Saldo ou limite insuficiente.", "schema": { "$ref": "#/definitions/ErrorResponse" } } } } }, "/cards/{cardId}/payment": { "post": { "summary": "Realizar pagamento de fatura do cartão de crédito", "description": "Paga um valor da fatura do cartão de crédito, debitando de uma conta corrente do cliente.", "security": [ { "bearerAuth": [] } ], "consumes": [ "application/json" ], "produces": [ "application/json" ], "parameters": [ { "name": "cardId", "in": "path", "required": true, "type": "string", "format": "uuid", "description": "ID do cartão de crédito para o qual a fatura será paga." }, { "name": "paymentRequest", "in": "body", "required": true, "schema": { "$ref": "#/definitions/CardPaymentRequest" } } ], "responses": { "200": { "description": "Pagamento de fatura realizado com sucesso.", "schema": { "$ref": "#/definitions/TransactionResponse" } }, "400": { "description": "Requisição inválida (e.g., valor negativo, conta de origem inválida, cartão não é de crédito).", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "401": { "description": "Não autenticado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "403": { "description": "Não autorizado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "404": { "description": "Cartão ou conta de origem não encontrados.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "409": { "description": "Saldo insuficiente na conta de origem para o pagamento.", "schema": { "$ref": "#/definitions/ErrorResponse" } } } } }, "/cards/{cardId}/invoice": { "get": { "summary": "Consultar fatura de um cartão de crédito", "description": "Retorna os detalhes da fatura atual (ou fechada) de um cartão de crédito específico.", "security": [ { "bearerAuth": [] } ], "produces": [ "application/json" ], "parameters": [ { "name": "cardId", "in": "path", "required": true, "type": "string", "format": "uuid", "description": "ID do cartão de crédito." }, { "name": "month", "in": "query", "description": "Mês da fatura (1-12). Padrão: Mês atual.", "required": false, "type": "integer", "minimum": 1, "maximum": 12 }, { "name": "year", "in": "query", "description": "Ano da fatura. Padrão: Ano atual.", "required": false, "type": "integer" } ], "responses": { "200": { "description": "Detalhes da fatura.", "schema": { "$ref": "#/definitions/CardInvoiceResponse" } }, "401": { "description": "Não autenticado.", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "403": { "description": "Não autorizado (cartão não pertence ao cliente logado ou não é de crédito).", "schema": { "$ref": "#/definitions/ErrorResponse" } }, "404": { "description": "Cartão ou fatura não encontrados.", "schema": { "$ref": "#/definitions/ErrorResponse" } } } } } }, "definitions": { "OnboardingRequest": { "type": "object", "required": [ "name", "email", "cpf", "password", "accountTypeToCreate" ], "properties": { "name": { "type": "string", "example": "Mariana Souza" }, "email": { "type": "string", "format": "email", "example": "mariana.souza@email.com" }, "cpf": { "type": "string", "pattern": "^\\d{3}\\.\\d{3}\\.\\d{3}-\\d{2}$", "example": "987.654.321-00" }, "password": { "type": "string", "format": "password", "example": "SenhaForteDigital!" }, "accountTypeToCreate": { "type": "array", "items": { "type": "string", "enum": ["CORRENTE", "POUPANCA"] }, "minItems": 1, "example": ["CORRENTE", "POUPANCA"] } } }, "OnboardingResponse": { "type": "object", "properties": { "userId": { "type": "string", "format": "uuid", "example": "a1b2c3d4-e5f6-7890-1234-567890abcdef" }, "message": { "type": "string", "example": "Cliente e conta(s) criados com sucesso!" }, "token": { "type": "string", "example": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." }, "accounts": { "type": "array", "items": { "$ref": "#/definitions/AccountResponse" } } } }, "LoginRequest": { "type": "object", "required": [ "email", "password" ], "properties": { "email": { "type": "string", "format": "email", "example": "mariana.souza@email.com" }, "password": { "type": "string", "format": "password", "example": "SenhaForteDigital!" } } }, "LoginResponse": { "type": "object", "properties": { "token": { "type": "string", "example": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." }, "expiresIn": { "type": "integer", "example": 3600, "description": "Tempo em segundos até o token expirar." } } }, "AccountResponse": { "type": "object", "properties": { "id": { "type": "string", "format": "uuid", "example": "f1e2d3c4-b5a6-9876-5432-10fedcba9876" }, "agency": { "type": "string", "example": "0001" }, "accountNumber": { "type": "string", "example": "12345-6" }, "accountType": { "type": "string", "enum": ["CORRENTE", "POUPANCA"], "example": "CORRENTE" }, "balance": { "type": "number", "format": "float", "example": 1500.75 }, "status": { "type": "string", "enum": ["ACTIVE", "INACTIVE", "BLOCKED"], "example": "ACTIVE" }, "createdAt": { "type": "string", "format": "date-time" } } }, "BalanceResponse": { "type": "object", "properties": { "accountId": { "type": "string", "format": "uuid", "example": "f1e2d3c4-b5a6-9876-5432-10fedcba9876" }, "balance": { "type": "number", "format": "float", "example": 2500.50 } } }, "DepositWithdrawRequest": { "type": "object", "required": [ "accountId", "amount" ], "properties": { "accountId": { "type": "string", "format": "uuid", "description": "ID da conta para realizar a operação." }, "amount": { "type": "number", "format": "float", "minimum": 0